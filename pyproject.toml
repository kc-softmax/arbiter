[tool.poetry]
name = "api"
version = "0.1.0"
description = ""
authors = ["jaehong0721 <newbie302@gmail.com>"]
readme = "README.md"
packages = [{include = "api", from = "src"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
